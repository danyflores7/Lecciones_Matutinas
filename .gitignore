# Dependencias
node_modules/

# Entorno (claves locales)
.env

# Sistema
.DS_Store
*.log

# Expo / build (además del .gitignore de mobile/)
mobile/.expo/
mobile/dist/
mobile/web-build/

# APKs generados
*.apk

# Audio generado (se publica en el branch 'audio' para el CDN, no en main)
tools/gen-audio/out/

# Llave de subida y sus claves (NUNCA subir)
*.jks
*.keystore
CLAVES-keystore.txt
*.aab

# Documento privado (lleva nombres y firmas) — NUNCA subir
legal/autorizacion-iglesia.md
